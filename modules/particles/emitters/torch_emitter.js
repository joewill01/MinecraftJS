class TorchEmitter extends Emitter{
	constructor(x,y,z){
		super(x,y,z)

		this.name="TorchEmitter"
		this.init();
	}

	tick(){
		//Check if we should emit. Determines delay
	}

	emit(){
		//Create the particle
	}
}